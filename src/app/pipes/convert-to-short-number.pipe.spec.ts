import { ConvertToShortNumberPipe } from './convert-to-short-number.pipe';

describe('ConvertToShortNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToShortNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
